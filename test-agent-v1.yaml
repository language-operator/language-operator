apiVersion: langop.io/v1alpha1
kind: LanguageAgent
metadata:
  name: test-v1-agent
  namespace: language-operator
spec:
  instructions: "You are a test agent to verify v1 versioning from initial synthesis. Your task is to respond to user requests with helpful information."
  image: ghcr.io/language-operator/agent:latest
  modelRefs:
    - name: qwen3-coder-30b
  toolRefs:
    - name: workspace