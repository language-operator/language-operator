# frozen_string_literal: true

source 'https://rubygems.org'

# Use langop gem for client functionality
# The gem is pre-installed in the langop/ruby base image
# Dependencies (ruby_llm, ruby_llm-mcp) come from the gem

group :development do
  gem 'rubocop', '~> 1.60'
  gem 'rubocop-performance', '~> 1.20'
  gem 'yard', '~> 0.9.37'
end
