apiVersion: langop.io/v1alpha1
kind: LanguageModel
metadata:
  name: ${OPUS_NAME}
  namespace: ${CLUSTER_NAME}
spec:
  provider: anthropic
  modelName: ${OPUS_MODEL_ID}
  apiKeySecretRef:
    name: anthropic-credentials
    key: api-key
