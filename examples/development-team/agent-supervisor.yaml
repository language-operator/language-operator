# Requires:
#   kubectl create secret generic github-credentials --from-literal=token=ghp_... -n development-team
#
# After deploy, open the agent terminal and run `/login` inside Claude Code to authenticate.
# Credentials persist on the workspace PVC across restarts.
apiVersion: langop.io/v1alpha1
kind: LanguageAgent
metadata:
  name: supervisor
  namespace: development-team
spec:
  runtime: claude-code
  persona: project-manager
  models:
    - name: claude-sonnet
  instructions: |
    You are the supervisor for the Language Operator engineering team.

    When triggered, run one delegation pass:
    1. If /workspace/language-operator does not exist, clone the repo:
         git clone https://github.com/language-operator/language-operator /workspace/language-operator
    2. Change to /workspace/language-operator
    3. List open unassigned issues: gh issue list --no-assignee --state open --limit 100
    4. For each issue, decide its queue:
         queue/0 — urgent: blocking bugs, failing CI, security issues
         queue/1 — normal: features, improvements, standard bugs
         queue/2 — backlog: chores, docs, low-priority cleanup
    5. Apply the queue label: gh issue edit <n> --add-label <queue>
    6. Print a summary of what was delegated and why.

    Use `gh` for all GitHub operations. Always work from /workspace/language-operator.
  networkPolicies:
    egress:
      - to:
          - cidr: "0.0.0.0/0"
        ports:
          - port: 443
            protocol: TCP
          - port: 80
            protocol: TCP
  workspace:
    size: 5Gi
  deployment:
    env:
      - name: GITHUB_TOKEN
        valueFrom:
          secretKeyRef:
            name: github-credentials
            key: token
