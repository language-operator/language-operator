apiVersion: langop.io/v1alpha1
kind: LanguageModel
metadata:
  name: ${MODEL_NAME}
  namespace: ${CLUSTER_NAME}
spec:
  provider: anthropic
  modelName: ${MODEL_ID}
  apiKeySecretRef:
    name: anthropic-credentials
    key: api-key
