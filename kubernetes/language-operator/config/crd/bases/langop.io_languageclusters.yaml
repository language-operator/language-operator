---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: languageclusters.langop.io
spec:
  group: langop.io
  names:
    kind: LanguageCluster
    listKind: LanguageClusterList
    plural: languageclusters
    singular: languagecluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.namespace
      name: Namespace
      type: string
    - jsonPath: .status.ciliumStatus.ready
      name: Cilium
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LanguageCluster is the Schema for the languageclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LanguageClusterSpec defines the desired state of LanguageCluster
            properties:
              groups:
                description: Security groups defining network boundaries
                items:
                  description: SecurityGroup defines a network isolation boundary
                  properties:
                    description:
                      description: Description of this group's purpose
                      type: string
                    egress:
                      description: Egress rules (where this group can connect TO)
                      items:
                        description: NetworkRule defines a single network policy rule
                        properties:
                          description:
                            description: Description of this rule
                            type: string
                          from:
                            description: From selector for ingress rules
                            properties:
                              cidr:
                                description: CIDR block
                                type: string
                              dns:
                                description: |-
                                  DNS names (supports wildcards with *)
                                  Examples: "api.openai.com", "*.googleapis.com"
                                items:
                                  type: string
                                type: array
                              group:
                                description: Group within the same LanguageCluster
                                type: string
                              namespaceSelector:
                                description: Namespace selector (for cross-namespace
                                  rules)
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                              podSelector:
                                description: Pod selector (within namespace)
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                              service:
                                description: Kubernetes service reference
                                properties:
                                  name:
                                    description: Service name
                                    type: string
                                  namespace:
                                    description: Service namespace (defaults to same
                                      namespace if omitted)
                                    type: string
                                required:
                                - name
                                type: object
                            type: object
                          ports:
                            description: Ports allowed by this rule
                            items:
                              description: NetworkPort defines a port and protocol
                              properties:
                                port:
                                  description: Port number
                                  format: int32
                                  maximum: 65535
                                  minimum: 1
                                  type: integer
                                protocol:
                                  default: TCP
                                  description: Protocol (TCP, UDP, SCTP)
                                  enum:
                                  - TCP
                                  - UDP
                                  - SCTP
                                  type: string
                              required:
                              - port
                              type: object
                            type: array
                          to:
                            description: To selector for egress rules
                            properties:
                              cidr:
                                description: CIDR block
                                type: string
                              dns:
                                description: |-
                                  DNS names (supports wildcards with *)
                                  Examples: "api.openai.com", "*.googleapis.com"
                                items:
                                  type: string
                                type: array
                              group:
                                description: Group within the same LanguageCluster
                                type: string
                              namespaceSelector:
                                description: Namespace selector (for cross-namespace
                                  rules)
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                              podSelector:
                                description: Pod selector (within namespace)
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                              service:
                                description: Kubernetes service reference
                                properties:
                                  name:
                                    description: Service name
                                    type: string
                                  namespace:
                                    description: Service namespace (defaults to same
                                      namespace if omitted)
                                    type: string
                                required:
                                - name
                                type: object
                            type: object
                        type: object
                      type: array
                    ingress:
                      description: Ingress rules (who can connect TO this group)
                      items:
                        description: NetworkRule defines a single network policy rule
                        properties:
                          description:
                            description: Description of this rule
                            type: string
                          from:
                            description: From selector for ingress rules
                            properties:
                              cidr:
                                description: CIDR block
                                type: string
                              dns:
                                description: |-
                                  DNS names (supports wildcards with *)
                                  Examples: "api.openai.com", "*.googleapis.com"
                                items:
                                  type: string
                                type: array
                              group:
                                description: Group within the same LanguageCluster
                                type: string
                              namespaceSelector:
                                description: Namespace selector (for cross-namespace
                                  rules)
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                              podSelector:
                                description: Pod selector (within namespace)
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                              service:
                                description: Kubernetes service reference
                                properties:
                                  name:
                                    description: Service name
                                    type: string
                                  namespace:
                                    description: Service namespace (defaults to same
                                      namespace if omitted)
                                    type: string
                                required:
                                - name
                                type: object
                            type: object
                          ports:
                            description: Ports allowed by this rule
                            items:
                              description: NetworkPort defines a port and protocol
                              properties:
                                port:
                                  description: Port number
                                  format: int32
                                  maximum: 65535
                                  minimum: 1
                                  type: integer
                                protocol:
                                  default: TCP
                                  description: Protocol (TCP, UDP, SCTP)
                                  enum:
                                  - TCP
                                  - UDP
                                  - SCTP
                                  type: string
                              required:
                              - port
                              type: object
                            type: array
                          to:
                            description: To selector for egress rules
                            properties:
                              cidr:
                                description: CIDR block
                                type: string
                              dns:
                                description: |-
                                  DNS names (supports wildcards with *)
                                  Examples: "api.openai.com", "*.googleapis.com"
                                items:
                                  type: string
                                type: array
                              group:
                                description: Group within the same LanguageCluster
                                type: string
                              namespaceSelector:
                                description: Namespace selector (for cross-namespace
                                  rules)
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                              podSelector:
                                description: Pod selector (within namespace)
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                              service:
                                description: Kubernetes service reference
                                properties:
                                  name:
                                    description: Service name
                                    type: string
                                  namespace:
                                    description: Service namespace (defaults to same
                                      namespace if omitted)
                                    type: string
                                required:
                                - name
                                type: object
                            type: object
                        type: object
                      type: array
                    name:
                      description: Name of the security group
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
              namespace:
                description: |-
                  Namespace to create/use for this cluster
                  If empty, auto-generates: <cluster-name>-ns
                type: string
              network:
                description: Network configuration
                properties:
                  cilium:
                    description: Cilium configuration
                    properties:
                      version:
                        description: Version of Cilium (for documentation/verification
                          purposes)
                        type: string
                    type: object
                  defaultPolicy:
                    default: deny
                    description: 'DefaultPolicy: deny (default) or allow'
                    enum:
                    - deny
                    - allow
                    type: string
                  podCIDR:
                    description: PodCIDR for documentation/validation purposes
                    type: string
                type: object
            required:
            - groups
            - network
            type: object
          status:
            description: LanguageClusterStatus defines the observed state
            properties:
              ciliumPolicies:
                description: CiliumNetworkPolicies created
                items:
                  type: string
                type: array
              ciliumStatus:
                description: Cilium installation status
                properties:
                  installed:
                    description: Installed indicates if Cilium is present
                    type: boolean
                  ready:
                    description: Ready indicates Cilium is fully operational
                    type: boolean
                  version:
                    description: Version of Cilium detected/installed
                    type: string
                required:
                - installed
                - ready
                type: object
              conditions:
                description: Conditions
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              groupMembership:
                additionalProperties:
                  description: GroupMembershipInfo tracks resources in a security
                    group
                  properties:
                    count:
                      description: Count of resources in this group
                      type: integer
                    resources:
                      description: List of resource names
                      items:
                        type: string
                      type: array
                  required:
                  - count
                  type: object
                description: Group membership tracking
                type: object
              namespace:
                description: Namespace created/used by this cluster
                type: string
              networkPolicies:
                description: NetworkPolicies created
                items:
                  type: string
                type: array
              phase:
                description: Phase of the cluster (Pending, Ready, Failed)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
