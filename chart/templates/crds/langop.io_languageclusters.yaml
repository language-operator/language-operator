---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
    controller-gen.kubebuilder.io/version: v0.14.0
  name: languageclusters.langop.io
spec:
  group: langop.io
  names:
    kind: LanguageCluster
    listKind: LanguageClusterList
    plural: languageclusters
    singular: languagecluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LanguageCluster is the Schema for the languageclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LanguageClusterSpec defines the desired state of LanguageCluster
            properties:
              capacity:
                description: |-
                  Capacity declares hard limits enforced via a ResourceQuota in the cluster's namespace.
                  When set, the controller creates a ResourceQuota named "langop-quota".
                  When unset, any existing "langop-quota" is deleted.
                properties:
                  maxAgents:
                    description: MaxAgents is the maximum number of LanguageAgent
                      objects allowed.
                    format: int32
                    type: integer
                  maxCPU:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      MaxCPU is the aggregate CPU limit for all pods in the cluster namespace.
                      Maps to limits.cpu in the namespace ResourceQuota.
                      Example: "4", "2500m"
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  maxMemory:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      MaxMemory is the aggregate memory limit for all pods in the cluster namespace.
                      Maps to limits.memory in the namespace ResourceQuota.
                      Example: "8Gi", "512Mi"
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  maxModels:
                    description: MaxModels is the maximum number of LanguageModel
                      objects allowed.
                    format: int32
                    type: integer
                  maxPersonas:
                    description: MaxPersonas is the maximum number of LanguagePersona
                      objects allowed.
                    format: int32
                    type: integer
                  maxTools:
                    description: MaxTools is the maximum number of LanguageTool objects
                      allowed.
                    format: int32
                    type: integer
                type: object
              domain:
                description: |-
                  Domain is the base domain for the cluster and agent webhook routing
                  The domain itself serves as the cluster dashboard/UI endpoint
                  Agent webhooks will be accessible at <uuid>.<domain>
                  Example: "ai.theryans.io" results in webhooks like "abc123.ai.theryans.io"
                type: string
              ingressConfig:
                description: IngressConfig defines ingress/gateway configuration for
                  the cluster
                properties:
                  gatewayClassName:
                    description: |-
                      Deprecated: Use GatewayName instead. This field actually refers to a Gateway resource name, not a GatewayClass.
                      GatewayClassName specifies the Gateway API GatewayClass to use
                      If empty, will attempt auto-detection or fall back to Ingress
                    type: string
                  gatewayName:
                    description: |-
                      GatewayName specifies the Gateway resource name to use
                      If empty, will attempt auto-detection or fall back to Ingress
                    type: string
                  gatewayNamespace:
                    description: |-
                      GatewayNamespace specifies the namespace of the Gateway resource
                      If empty, defaults to the same namespace as the LanguageCluster
                    type: string
                  ingressClassName:
                    description: |-
                      IngressClassName specifies the Ingress class to use for fallback
                      Only used when Gateway API is not available
                    type: string
                  tls:
                    description: TLS configuration for agent webhooks
                    properties:
                      enabled:
                        default: true
                        description: Enabled controls whether TLS is enabled for webhooks
                        type: boolean
                      issuerRef:
                        description: IssuerRef references a cert-manager Issuer or
                          ClusterIssuer
                        properties:
                          group:
                            default: cert-manager.io
                            description: Group is the API group of the issuer
                            type: string
                          kind:
                            default: ClusterIssuer
                            description: Kind is either "Issuer" or "ClusterIssuer"
                            enum:
                            - Issuer
                            - ClusterIssuer
                            type: string
                          name:
                            description: Name of the Issuer or ClusterIssuer
                            type: string
                        required:
                        - name
                        type: object
                      secretName:
                        description: |-
                          SecretName is the name of the TLS secret (for manual cert management)
                          If empty, cert-manager will be used if available
                        type: string
                    type: object
                type: object
              networkPolicies:
                description: NetworkPolicies defines egress network policies for agents
                  in this cluster
                items:
                  description: NetworkRule defines a single network policy rule
                  properties:
                    description:
                      description: Description of this rule
                      type: string
                    from:
                      description: From selector for ingress rules
                      properties:
                        cidr:
                          description: CIDR block
                          type: string
                        dns:
                          description: |-
                            DNS names (supports wildcards with *)
                            Examples: "api.openai.com", "*.googleapis.com"
                          items:
                            type: string
                          type: array
                        group:
                          description: |-
                            Group selects pods with matching langop.io/group label
                            Used to allow communication with specific labeled resources
                          type: string
                        namespaceSelector:
                          description: Namespace selector (for cross-namespace rules)
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        podSelector:
                          description: Pod selector (within namespace)
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        service:
                          description: Kubernetes service reference
                          properties:
                            name:
                              description: Service name
                              type: string
                            namespace:
                              description: Service namespace (defaults to same namespace
                                if omitted)
                              type: string
                          required:
                          - name
                          type: object
                      type: object
                    ports:
                      description: Ports allowed by this rule
                      items:
                        description: NetworkPort defines a port and protocol
                        properties:
                          port:
                            description: Port number
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          protocol:
                            default: TCP
                            description: Protocol (TCP, UDP, SCTP)
                            enum:
                            - TCP
                            - UDP
                            - SCTP
                            type: string
                        required:
                        - port
                        type: object
                      type: array
                    to:
                      description: To selector for egress rules
                      properties:
                        cidr:
                          description: CIDR block
                          type: string
                        dns:
                          description: |-
                            DNS names (supports wildcards with *)
                            Examples: "api.openai.com", "*.googleapis.com"
                          items:
                            type: string
                          type: array
                        group:
                          description: |-
                            Group selects pods with matching langop.io/group label
                            Used to allow communication with specific labeled resources
                          type: string
                        namespaceSelector:
                          description: Namespace selector (for cross-namespace rules)
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        podSelector:
                          description: Pod selector (within namespace)
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        service:
                          description: Kubernetes service reference
                          properties:
                            name:
                              description: Service name
                              type: string
                            namespace:
                              description: Service namespace (defaults to same namespace
                                if omitted)
                              type: string
                          required:
                          - name
                          type: object
                      type: object
                  type: object
                type: array
              proxy:
                description: Proxy configures the shared LiteLLM proxy deployed per
                  cluster
                properties:
                  ingressEnabled:
                    description: |-
                      IngressEnabled controls whether an Ingress/HTTPRoute is created for the proxy.
                      Defaults to true when cluster.spec.domain is set.
                    type: boolean
                  replicas:
                    default: 1
                    description: Replicas sets the number of proxy pod replicas.
                    format: int32
                    type: integer
                  resources:
                    description: Resources sets CPU/memory requests and limits for
                      the proxy Deployment.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.


                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.


                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
            type: object
          status:
            description: LanguageClusterStatus defines the observed state
            properties:
              capacity:
                description: Capacity reports observed resource usage in this cluster's
                  namespace.
                properties:
                  agentCount:
                    description: AgentCount is the number of LanguageAgent objects
                      in the cluster namespace.
                    format: int32
                    type: integer
                  modelCount:
                    description: ModelCount is the number of LanguageModel objects
                      in the cluster namespace.
                    format: int32
                    type: integer
                  personaCount:
                    description: PersonaCount is the number of LanguagePersona objects
                      in the cluster namespace.
                    format: int32
                    type: integer
                  toolCount:
                    description: ToolCount is the number of LanguageTool objects in
                      the cluster namespace.
                    format: int32
                    type: integer
                  totalCPULimits:
                    anyOf:
                    - type: integer
                    - type: string
                    description: TotalCPULimits is the sum of limits.cpu across all
                      agent pod specs.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  totalMemoryLimits:
                    anyOf:
                    - type: integer
                    - type: string
                    description: TotalMemoryLimits is the sum of limits.memory across
                      all agent pod specs.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                required:
                - agentCount
                - modelCount
                - personaCount
                - toolCount
                type: object
              conditions:
                description: Conditions
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase of the cluster (Pending, Ready, Failed)
                type: string
              proxyEndpoint:
                description: ProxyEndpoint is the in-cluster URL for the shared LiteLLM
                  proxy
                type: string
              proxyReady:
                description: ProxyReady indicates whether the shared proxy Deployment
                  is available
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
